import React from 'react';
import {NO_IMAGE_URL} from '../../../config/config';
const NoImage = (props) =>{
   return <img src={NO_IMAGE_URL} className="group list-group-image img-fluid" alt="no image"/>
}

export default NoImage;